let succ = function(x) x +1 ;
let yes = function() true;
let no = function() false;

data.sort(function(a,b) b - a);
let sumOfSquares = funciton(data) Array.reduce(Array.map(data, function(x) x * x), function(x,y) x + y);

